export declare class NthLargestNumberFinderService {
    calculateNthLargestFinder(array: number[], position: number): number;
    find(inputArray: number[], requiredPosition: number, callbackFn: (arr: number[], position: number) => number): number;
}
